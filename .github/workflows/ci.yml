name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  python:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      # The interop test self-skips here because mcp-server/node_modules
      # is absent; that is expected. The interop job covers it.
      - name: Run CLI tests
        run: python -m unittest discover -s tests -v

  node:
    name: Node ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 24]
    defaults:
      run:
        working-directory: mcp-server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: mcp-server/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Run MCP server tests
        run: npm test

  interop:
    name: CLI and MCP interop
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mcp-server/package-lock.json
      - name: Install MCP server dependencies
        run: npm ci
        working-directory: mcp-server
      # With node_modules present, tests/test_interop.py exercises the CLI
      # and the MCP server against the same state directory.
      - name: Run full test suite including interop
        run: python -m unittest discover -s tests -v

  hygiene:
    name: Repository hygiene
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: actions/setup-node@v6
        with:
          node-version: 20
      - name: Generated files are in sync
        run: |
          python scripts/build_variants.py
          git diff --exit-code CLAUDE.md AGENTS.md .cursorrules
          node scripts/build_registry_docs.mjs
          git diff --exit-code docs/adapter-candidates.md
      - name: ASCII rules
        run: |
          python - <<'PY'
          import subprocess
          import sys

          SKIP = {"docs/research-report.md"}

          def is_banned(codepoint):
              if codepoint in (0x2013, 0x2014, 0xFE0F):
                  return True
              if 0x1F000 <= codepoint <= 0x1FAFF:
                  return True
              if 0x2600 <= codepoint <= 0x27BF:
                  return True
              if 0x2B00 <= codepoint <= 0x2BFF:
                  return True
              return False

          listing = subprocess.run(
              ["git", "ls-files"],
              capture_output=True,
              text=True,
              check=True,
          )
          problems = []
          for path in listing.stdout.splitlines():
              if path in SKIP:
                  continue
              try:
                  with open(path, encoding="utf-8") as handle:
                      lines = handle.readlines()
              except (OSError, UnicodeDecodeError):
                  continue
              for lineno, line in enumerate(lines, start=1):
                  for char in line:
                      codepoint = ord(char)
                      if is_banned(codepoint):
                          problems.append(
                              "{0}:{1}: U+{2:04X}".format(path, lineno, codepoint)
                          )
          if problems:
              print("Banned characters found (dashes or emoji):")
              for problem in problems:
                  print(problem)
              sys.exit(1)
          print("ASCII rules check passed.")
          PY
